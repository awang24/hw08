package cs3500.animator.provider.model;

/**
 * Represent a enumeration for shape type. Operation can only be move or changeColor or scale (for
 * now).
 */
public enum OperationType {
  move, changeColor, scale
}
